generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Task {
  id     Int       @id @default(autoincrement())
  listId Int
  name   String
  list   TasksList @relation(fields: [listId], references: [id])
}

model TasksList {
  id      Int         @id @default(autoincrement())
  name    String
  boardId Int
  tasks   Task[]
  board   KanbanBoard @relation(fields: [boardId], references: [id])
}

model KanbanBoard {
  id    Int         @id @default(autoincrement())
  name  String
  lists TasksList[]
}
