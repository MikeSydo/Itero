# Quick Start Guide - Itero

## 📋 Перше встановлення

### 1. Встановіть залежності

```bash
npm install
```

### 2. Налаштуйте базу даних

Створіть `.env` файл:
```bash
cp .env.example .env
```

Відредагуйте `.env` та вкажіть свій DATABASE_URL:
```env
DATABASE_URL="postgresql://postgres:password@localhost:5432/itero?schema=public"
API_PORT=3001
```

### 3. Запустіть міграції

```bash
npm run prisma:generate
npm run prisma:migrate
```

## 🚀 Запуск додатку

### Варіант 1: Web версія (браузер)

```bash
npm run dev
```

Це запустить:
- ✅ Frontend на http://localhost:8000
- ✅ Backend API на http://localhost:3001

Відкрийте браузер: http://localhost:8000

### Варіант 2: Desktop версія (Electron)

```bash
npm run dev:electron
```

Це запустить:
- ✅ Frontend dev server
- ✅ Backend API server
- ✅ Electron вікно з додатком

Вікно додатку відкриється автоматично.

## 🎯 Що можна робити

### В додатку:

1. **Welcome сторінка** - огляд функціоналу
2. **Boards** (меню зліва) - список всіх Kanban дошок
3. **Переглянути дошку** - клікнути на картку дошки
4. **Додати завдання** - кнопка "+ Create" в списку
5. **Видалити завдання** - меню "..." на картці
6. **Видалити список** - меню "..." на заголовку списку

### В Prisma Studio (опціонально):

```bash
npm run prisma:studio
```

Відкриється веб-інтерфейс для управління БД: http://localhost:5555

## 📦 Збірка Production

### Web версія

```bash
npm run build
```

Файли з'являться в папці `dist/`

### Desktop версія

```bash
# Для поточної платформи
npm run build:electron

# Або для конкретної:
npm run electron:dist:win    # Windows
npm run electron:dist:mac    # macOS
npm run electron:dist:linux  # Linux
```

Готові інсталятори з'являться в папці `dist/`

## 🔧 Корисні команди

```bash
# Перезапустити тільки frontend
npm run dev:frontend

# Перезапустити тільки backend
npm run api:dev

# Перевірка коду
npm run lint

# TypeScript перевірка
npm run tsc

# Очистити кеш і перезібрати
rm -rf node_modules dist dist-electron .umi
npm install
```

## ❓ Проблеми

### Backend не запускається

```bash
# Перевірте чи запущена PostgreSQL
# Перевірте DATABASE_URL в .env
# Перезапустіть міграції:
npm run prisma:generate
npm run prisma:migrate
```

### Electron не відкривається

```bash
# Скомпілюйте Electron файли:
npm run electron:transpile

# Перевірте чи запущений dev server на :8000
# Запустіть знову:
npm run dev:electron
```

### Порт :8000 зайнятий

```bash
# Змініть порт в config/config.ts
# Або зупиніть інший процес на :8000
```

### Помилки TypeScript

```bash
# Перевірте всі помилки:
npm run tsc

# Більшість можна виправити автоматично:
npm run lint:fix
```

## 📚 Документація

- **README.md** - головна документація
- **MIGRATION.md** - інформація про міграцію проекту
- **ELECTRON.md** - детальний гайд по Electron

## 🎓 Наступні кроки

1. Вивчіть структуру проекту
2. Подивіться як працюють API сервіси в `src/services/itero/`
3. Додайте нові компоненти в `src/components/`
4. Створіть нові сторінки в `src/pages/`
5. Розширте API в `api/app.ts`
6. Оновіть Prisma схему в `prisma/schema.prisma`

## 💡 Поради

- Використовуйте DevTools в Electron (відкриваються автоматично в dev mode)
- Prisma Studio допоможе швидко додати тестові дані
- Змінюйте код - hot reload працює і для frontend, і для backend
- Перевіряйте console в браузері для помилок

## 🤝 Додаткова допомога

- [Ant Design Pro Docs](https://pro.ant.design)
- [Electron Documentation](https://www.electronjs.org/docs)
- [Prisma Guides](https://www.prisma.io/docs)

Успішної розробки! 🚀
